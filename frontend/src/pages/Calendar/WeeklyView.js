import React from 'react';

const WeeklyView = ({date}) => {
    return (
        <div>
            Todo: Weekly View, probably mimic Google Calendar's 7 columns
        </div>
    );
}
 
export default WeeklyView;